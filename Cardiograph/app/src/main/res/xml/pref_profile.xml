<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- NOTE: EditTextPreference accepts EditText attributes. -->
    <!-- NOTE: EditTextPreference's summary should be set to its value by the activity code. -->
    <EditTextPreference
        android:capitalize="words"
        android:defaultValue=""
        android:inputType="textCapWords"
        android:key="user_name"
        android:maxLines="1"
        android:selectAllOnFocus="true"
        android:singleLine="true"
        android:title="Name" />

    <!-- NOTE: Hide buttons to simplify the UI. Users can touch outside the dialog to
     dismiss it. -->
    <!-- NOTE: ListPreference's summary should be set to its value by the activity code. -->
    <ListPreference
        android:entries="@array/gender_list_titles"
        android:entryValues="@array/gender_list_values"
        android:key="gender_list"
        android:negativeButtonText="@null"
        android:positiveButtonText="@null"
        android:title="Gender" />

    <EditTextPreference
        android:key="age"
        android:inputType="number"
        android:title="Age" />

    <EditTextPreference
        android:key="weight"
        android:inputType="number"
        android:title="Weight (kg)" />

    <EditTextPreference
        android:key="max_heart_rate"
        android:inputType="number"
        android:title="Max Heart Rate (bpm)" />

    <EditTextPreference
        android:key="max_heart_rate_duration"
        android:inputType="number"
        android:defaultValue="15"
        android:summary="If your Heart Rate exceeds your maximum set value for this duration (secs) then your heart may need a check up and your emergency contacts are alerted."
        android:title="Max Heart Rate Duration (seconds)" />

    <EditTextPreference
        android:key="min_heart_rate"
        android:inputType="number"
        android:title="Min Heart Rate (bpm)" />

    <EditTextPreference
        android:key="min_heart_rate_duration"
        android:inputType="number"
        android:defaultValue="15"
        android:summary="If your Heart Rate exceeds your minimum set value for this duration (secs) then your heart may need a check up and your emergency contacts are alerted."
        android:title="Min Heart Rate Duration (seconds)" />

    <EditTextPreference
        android:key="max_rr_interval"
        android:inputType="number"
        android:defaultValue="4000"
        android:summary="This is the maximum time between each heart beat used as a reference for detecting problems with your heart beat variability and alerting your emergency contacts."
        android:title="Max RR interval (milliseconds)" />

    <EditTextPreference
        android:key="min_rr_interval"
        android:inputType="number"
        android:summary="This is the minimum time between each heart beat used as a reference for detecting problems with your heart beat variability and alerting your emergency contacts."
        android:defaultValue="200"
        android:title="Min RR interval (milliseconds)" />


</PreferenceScreen>
